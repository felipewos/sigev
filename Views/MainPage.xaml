<Page
    x:Class="PrimeiraTelaWinUI.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:PrimeiraTelaWinUI.Views"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Padding" Value="24" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
        </Style>

        <Style x:Key="SectionTitleTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="CaptionTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Opacity" Value="0.72" />
        </Style>
    </Page.Resources>

    <Grid Padding="32" RowSpacing="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="6">
            <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="Projetos escolares" />
            <TextBlock
                MaxWidth="880"
                Style="{StaticResource CaptionTextStyle}"
                Text="Selecione uma escola ou curso para continuar." />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}">
            <Grid RowSpacing="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2.4*" />
                        <ColumnDefinition Width="1.4*" />
                        <ColumnDefinition Width="1.2*" />
                        <ColumnDefinition Width="1.2*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock FontWeight="SemiBold" Text="Instituição" />
                    <TextBlock Grid.Column="1" FontWeight="SemiBold" Text="Curso" />
                    <TextBlock Grid.Column="2" FontWeight="SemiBold" Text="Criado em" />
                    <TextBlock Grid.Column="3" FontWeight="SemiBold" Text="Alterado em" />
                </Grid>

                <ListView
                    x:Name="ProjectsListView"
                    Grid.Row="1"
                    HorizontalContentAlignment="Stretch"
                    IsTabStop="True"
                    SelectionChanged="OnSelectionChanged"
                    DoubleTapped="OnProjectsListViewDoubleTapped"
                    KeyDown="OnProjectsListViewKeyDown"
                    SelectionMode="Single">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:ProjectItem">
                            <Grid MinHeight="64" Padding="0,12" ColumnSpacing="16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2.4*" />
                                    <ColumnDefinition Width="1.4*" />
                                    <ColumnDefinition Width="1.2*" />
                                    <ColumnDefinition Width="1.2*" />
                                </Grid.ColumnDefinitions>

                                <TextBlock
                                    Grid.Column="0"
                                    FontSize="15"
                                    FontWeight="SemiBold"
                                    Text="{x:Bind Name}"
                                    TextWrapping="WrapWholeWords" />
                                <TextBlock
                                    Grid.Column="1"
                                    Opacity="0.8"
                                    Text="{x:Bind Course}"
                                    TextWrapping="WrapWholeWords" />
                                <TextBlock Grid.Column="2" Opacity="0.8" Text="{x:Bind CreatedAtText}" />
                                <TextBlock Grid.Column="3" Opacity="0.8" Text="{x:Bind ModifiedAtText}" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <StackPanel
            Grid.Row="2"
            HorizontalAlignment="Right"
            Orientation="Horizontal"
            Spacing="12">
            <Button Content="Criar projeto escolar" Click="OnCreateClicked" />
            <Button
                x:Name="OpenButton"
                Click="OnOpenClicked"
                Content="Abrir"
                IsEnabled="False" />
            <Button
                x:Name="RenameButton"
                Click="OnRenameClicked"
                Content="Editar nome"
                IsEnabled="False" />
            <Button
                x:Name="DeleteButton"
                Click="OnDeleteClicked"
                Content="Excluir"
                IsEnabled="False" />
        </StackPanel>
    </Grid>
</Page>
